<div class="column"">
	<div class="invisible"></div>
	<% current_user.events.where(day: day).each do |e| %>
		<div id="e<%= e.id %>" data-id="<%= e.id %>"
			 class="event-class ui vertical padded segment <%= Event::COLORS[e.urgency+1] %> inverted">
			<h4 class="ui header"><%= e.title %></h4>
		</div>
	<% end %>
</div>